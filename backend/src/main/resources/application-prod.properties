# Production Configuration for EC2/Render
# Centralized configuration is in application.properties.
# Use this file for environment-specific overrides only.

# Example: If you want to use a different database in production
# spring.datasource.url=${DATABASE_URL}
# spring.datasource.username=${DATABASE_USER}
# spring.datasource.password=${DATABASE_PASSWORD}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Supabase PostgreSQL connection (set these in your host environment)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=jLjcryLmyU5j@8i
spring.datasource.driver-class-name=org.postgresql.Driver

# Server port for Render
server.port=${PORT:8080}
